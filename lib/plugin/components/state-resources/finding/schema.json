{
  "type": "object",
  "properties": {
    "modelId": {
      "type": "string",
      "description": "This is the id of a model from which zero or more documents will be fetched."
    },
    "filter": {
      "type": "object",
      "description": "Conditions which documents must meet to be returned."
    }

  },
  "required": ["modelId"],
  "additionalProperties": false
}
