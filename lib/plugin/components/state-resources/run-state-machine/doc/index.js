'use strict'

module.exports = {
  description: 'Run another state machine and wait for it to complete. The output of this state-resource is the output from the spawned state machine.',
  example: require('./example.json')
}
