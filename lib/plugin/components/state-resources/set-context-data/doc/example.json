{
  "SetContextData": {
    "Type": "Task",
    "Resource": "module:setContextData",
    "ResourceConfig": {
      "$.formData.sawReportId": "$.boardKeys.sawReportId",
      "$.formData.loggedInUsersEmail": "$EMAIL",
      "$.formData.loggedInUsersAuthId": "$USERID",
      "$.formData.currentTimestampUTC": "$NOW"
    },
    "Next": "AwaitingHumanInput"
  }
}
