'use strict'

module.exports = {
  description: 'Finds a single document in a specified model that meets the specified criteria, and adds it to the tymly context',
  example: require('./example.json')
}
